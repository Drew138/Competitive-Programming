
#include <bits/stdc++.h>

using namespace std;

bool is_palindrome(string s) {
    int i = 0, j = 3;
    while (i < j) {
        if (s[i] != s[j]) return false;
    }
    return true;
}


int main() {
    int t;
    cin >> t;

    while (t--) {
        string s;
        cin >> s;
        int x
    }
}